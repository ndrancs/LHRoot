#!/system/bin/sh

######### IMPORT BOOTLINUX ENVIRONMENT #########
SCRIPT_PATH=$(readlink -f $0)
. ${SCRIPT_PATH%/*}/bootlinux_env

if [ -n "$3" ]; then
   $busybox chroot $mnt /usr/bin/env su - $user
else
   $busybox chroot $mnt /usr/bin/env su $user -c '/bin/bash -c $cmd'
fi
